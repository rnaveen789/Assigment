## C Programs — Simple Examples

This repository contains small C programs for practice.

### Files
- `1.c`: Addition of two numbers
- `2.c`: (add a short description)

### Prerequisites
- GCC or any C compiler
- Windows PowerShell or a terminal
- Git (to version control and push to GitHub)

### Build and Run
# Compile
gcc 1.c -o add

# Run
./addFor `2.c`, replace file and output names accordingly.

### Git: Initialize, Commit, and Push
# From the project folder
cd "C:\Users\R Naveen\OneDrive\Desktop\new"

# Initialize (first time only)
git init

# Add files and commit
git add .
git commit -m "Initial commit: add C programs"

# Connect to GitHub (replace with your repo URL)
git remote add origin https://github.com/<your-username>/<your-repo>.git

# Push to main branch
git branch -M main
git push -u origin main### Pull Latest Changes
git pull### License
MIT (or your preferred license)
